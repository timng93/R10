import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    gradient: {
        margin: 15,
        borderRadius: 25,
        padding: 5
      },
      buttonTitle: {
        color: "#fff",
        textAlign: "center",
        fontSize: 16
      }
});

export default styles;
