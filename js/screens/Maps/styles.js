import { StyleSheet } from "react-native";
const styles = StyleSheet.create({
  container: {
    height: 700,
    width: 450
  },
  map: {
    height: 700,
    width: 450
  }
});

export default styles;
